class Program {
  public static boolean isPalindrome(String str) {
		// // Write your code here.
		for(int i=0,j=str.length()-1;i<=j;i++,j--)
				if(str.charAt(i)!=str.charAt(j))
					return false;
		return true;
		// return (new StringBuilder(str)).reverse().toString().matches(str);
  }
}
